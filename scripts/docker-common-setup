#!/bin/bash

echo "import cacerts..."
rsync -avu $HOME/work/etc/cacerts config/

if [ "$1" == "nosecrets" ]; then
  exit 0
fi

echo "import secrets..."
\cp ~/work/etc/RsaSignaturePub.key config/
\cp ~/work/test-certificates/servops.pem config/cadcproxy.pem

