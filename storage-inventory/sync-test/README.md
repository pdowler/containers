# storage site sync testing

This setup is to be able to run `fenwick` and `critwall` to sync
and then `ratik` and `tantar` to validate a storage site.

The target content is 2580 IRIS files requiring 1.9GiB.

